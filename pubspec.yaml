name: web_server
description: A static web server that serves pages configured via YAML.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  yaml: ^3.1.0
  path: ^1.9.0

executables:
  server: server
